/**
 * useCurrency — convenience re-export
 *
 * Import from here instead of the context file for cleaner imports in components.
 *
 * @example
 * import { useCurrency } from '../hooks/useCurrency';
 * const { format, currency, setCurrency } = useCurrency();
 */

export { useCurrency } from '../contexts/CurrencyContext.jsx';
