[workspace]
members = ["contracts/escrow_contract", "contracts/governance", "contracts/insurance_contract", "contracts/escrow_extensions", "contracts/shared"]
resolver = "2"

[profile.release]
opt-level = "z"
lto = "fat"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
